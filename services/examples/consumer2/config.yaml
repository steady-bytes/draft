service:
  name: consumer2
  domain: examples
  env: local
  entrypoint: http://localhost:2221
  logging:
    level: debug

  network:
    bind_address: localhost
    bind_port: 9094
    internal:
      host: localhost
      port: 9094
    external:
      host: localhost:10000

telemetry:
  enabled: true

catalyst:
  address: http://localhost:2220
