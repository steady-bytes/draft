[application]

# App (Project) Name
name = "blueprint pwa"

# Dioxus App Default Platform
# desktop, web, mobile, ssr
default_platform = "web"

# `build` & `serve` dist path
out_dir = "dist"

# resource (public) file folder
asset_dir = "public"

[web.app]

# HTML title tag content
title = "{blueprint}"

[web.watcher]

# when watcher trigger, regenerate the `index.html`
reload_html = true

# which files or dirs will be watcher monitoring
watch_path = ["src", "public"]

[web.resource.dev]

[[web.proxy]]
backend = "http://localhost:2221/core.registry.key_value.v1.KeyValueService/"

[[web.proxy]]
backend = "http://localhost:2221/core.registry.service_discovery.v1.ServiceDiscoveryService/"

[[web.proxy]]
backend = "http://localhost:18000/core.control_plane.networking.v1.NetworkingService/"