service:
  name: fuse
  domain: core
  entrypoint: http://localhost:2221

  logging:
    level: debug

  network:
    bind_port: 18000
    internal:
      host: localhost
      port: 18000

telemetry:
  enabled: true

fuse:
  address: http://localhost:18000
  listener:
    address: 0.0.0.0
    port: 10000
