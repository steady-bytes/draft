// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: examples/echo/v1/service.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/steady-bytes/draft/api/examples/echo/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// EchoServiceName is the fully-qualified name of the EchoService service.
	EchoServiceName = "examples.echo.v1.EchoService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// EchoServiceSpeakProcedure is the fully-qualified name of the EchoService's Speak RPC.
	EchoServiceSpeakProcedure = "/examples.echo.v1.EchoService/Speak"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	echoServiceServiceDescriptor     = v1.File_examples_echo_v1_service_proto.Services().ByName("EchoService")
	echoServiceSpeakMethodDescriptor = echoServiceServiceDescriptor.Methods().ByName("Speak")
)

// EchoServiceClient is a client for the examples.echo.v1.EchoService service.
type EchoServiceClient interface {
	Speak(context.Context, *connect.Request[v1.SpeakRequest]) (*connect.Response[v1.SpeakResponse], error)
}

// NewEchoServiceClient constructs a client for the examples.echo.v1.EchoService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEchoServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) EchoServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &echoServiceClient{
		speak: connect.NewClient[v1.SpeakRequest, v1.SpeakResponse](
			httpClient,
			baseURL+EchoServiceSpeakProcedure,
			connect.WithSchema(echoServiceSpeakMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// echoServiceClient implements EchoServiceClient.
type echoServiceClient struct {
	speak *connect.Client[v1.SpeakRequest, v1.SpeakResponse]
}

// Speak calls examples.echo.v1.EchoService.Speak.
func (c *echoServiceClient) Speak(ctx context.Context, req *connect.Request[v1.SpeakRequest]) (*connect.Response[v1.SpeakResponse], error) {
	return c.speak.CallUnary(ctx, req)
}

// EchoServiceHandler is an implementation of the examples.echo.v1.EchoService service.
type EchoServiceHandler interface {
	Speak(context.Context, *connect.Request[v1.SpeakRequest]) (*connect.Response[v1.SpeakResponse], error)
}

// NewEchoServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEchoServiceHandler(svc EchoServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	echoServiceSpeakHandler := connect.NewUnaryHandler(
		EchoServiceSpeakProcedure,
		svc.Speak,
		connect.WithSchema(echoServiceSpeakMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/examples.echo.v1.EchoService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EchoServiceSpeakProcedure:
			echoServiceSpeakHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEchoServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedEchoServiceHandler struct{}

func (UnimplementedEchoServiceHandler) Speak(context.Context, *connect.Request[v1.SpeakRequest]) (*connect.Response[v1.SpeakResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("examples.echo.v1.EchoService.Speak is not implemented"))
}
