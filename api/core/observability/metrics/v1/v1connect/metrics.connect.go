// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: core/observability/metrics/v1/metrics.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/steady-bytes/draft/api/core/observability/metrics/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// MetricsServiceName is the fully-qualified name of the MetricsService service.
	MetricsServiceName = "core.observability.metrics.v1.MetricsService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// MetricsServiceQueryMetricsProcedure is the fully-qualified name of the MetricsService's
	// QueryMetrics RPC.
	MetricsServiceQueryMetricsProcedure = "/core.observability.metrics.v1.MetricsService/QueryMetrics"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	metricsServiceServiceDescriptor            = v1.File_core_observability_metrics_v1_metrics_proto.Services().ByName("MetricsService")
	metricsServiceQueryMetricsMethodDescriptor = metricsServiceServiceDescriptor.Methods().ByName("QueryMetrics")
)

// MetricsServiceClient is a client for the core.observability.metrics.v1.MetricsService service.
type MetricsServiceClient interface {
	// QueryMetrics evaluates a scoped PromQL-subset expression (range-vector
	// selectors, rate(), sum/avg/max/min by(...), simple scalar arithmetic —
	// see services/core/beacon/query/promql.go) against `metric_points` over
	// [start, end], sampled at `step`, and returns one time series per
	// distinct label set the expression produces.
	QueryMetrics(context.Context, *connect.Request[v1.QueryMetricsRequest]) (*connect.Response[v1.QueryMetricsResponse], error)
}

// NewMetricsServiceClient constructs a client for the core.observability.metrics.v1.MetricsService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewMetricsServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) MetricsServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &metricsServiceClient{
		queryMetrics: connect.NewClient[v1.QueryMetricsRequest, v1.QueryMetricsResponse](
			httpClient,
			baseURL+MetricsServiceQueryMetricsProcedure,
			connect.WithSchema(metricsServiceQueryMetricsMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// metricsServiceClient implements MetricsServiceClient.
type metricsServiceClient struct {
	queryMetrics *connect.Client[v1.QueryMetricsRequest, v1.QueryMetricsResponse]
}

// QueryMetrics calls core.observability.metrics.v1.MetricsService.QueryMetrics.
func (c *metricsServiceClient) QueryMetrics(ctx context.Context, req *connect.Request[v1.QueryMetricsRequest]) (*connect.Response[v1.QueryMetricsResponse], error) {
	return c.queryMetrics.CallUnary(ctx, req)
}

// MetricsServiceHandler is an implementation of the core.observability.metrics.v1.MetricsService
// service.
type MetricsServiceHandler interface {
	// QueryMetrics evaluates a scoped PromQL-subset expression (range-vector
	// selectors, rate(), sum/avg/max/min by(...), simple scalar arithmetic —
	// see services/core/beacon/query/promql.go) against `metric_points` over
	// [start, end], sampled at `step`, and returns one time series per
	// distinct label set the expression produces.
	QueryMetrics(context.Context, *connect.Request[v1.QueryMetricsRequest]) (*connect.Response[v1.QueryMetricsResponse], error)
}

// NewMetricsServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewMetricsServiceHandler(svc MetricsServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	metricsServiceQueryMetricsHandler := connect.NewUnaryHandler(
		MetricsServiceQueryMetricsProcedure,
		svc.QueryMetrics,
		connect.WithSchema(metricsServiceQueryMetricsMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/core.observability.metrics.v1.MetricsService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case MetricsServiceQueryMetricsProcedure:
			metricsServiceQueryMetricsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedMetricsServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedMetricsServiceHandler struct{}

func (UnimplementedMetricsServiceHandler) QueryMetrics(context.Context, *connect.Request[v1.QueryMetricsRequest]) (*connect.Response[v1.QueryMetricsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("core.observability.metrics.v1.MetricsService.QueryMetrics is not implemented"))
}
