// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: core/observability/logs/v1/logs.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/steady-bytes/draft/api/core/observability/logs/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// LogsServiceName is the fully-qualified name of the LogsService service.
	LogsServiceName = "core.observability.logs.v1.LogsService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// LogsServiceQueryLogsProcedure is the fully-qualified name of the LogsService's QueryLogs RPC.
	LogsServiceQueryLogsProcedure = "/core.observability.logs.v1.LogsService/QueryLogs"
	// LogsServiceStreamLogsProcedure is the fully-qualified name of the LogsService's StreamLogs RPC.
	LogsServiceStreamLogsProcedure = "/core.observability.logs.v1.LogsService/StreamLogs"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	logsServiceServiceDescriptor          = v1.File_core_observability_logs_v1_logs_proto.Services().ByName("LogsService")
	logsServiceQueryLogsMethodDescriptor  = logsServiceServiceDescriptor.Methods().ByName("QueryLogs")
	logsServiceStreamLogsMethodDescriptor = logsServiceServiceDescriptor.Methods().ByName("StreamLogs")
)

// LogsServiceClient is a client for the core.observability.logs.v1.LogsService service.
type LogsServiceClient interface {
	// QueryLogs returns a bounded set of log rows matching an optional BeaconQL
	// filter expression, most recent first.
	QueryLogs(context.Context, *connect.Request[v1.QueryLogsRequest]) (*connect.Response[v1.QueryLogsResponse], error)
	// StreamLogs opens a persistent server-streaming RPC. The server first replays
	// historical rows matching `filter` (bounded by `limit`, optionally starting
	// after the `after` cursor), then continues streaming newly-ingested rows
	// matching the same filter as they arrive. The stream stays open until the
	// client closes it; the client re-issues the RPC with a new `filter` to change
	// the active filter, using the last received row's timestamp as the new
	// `after` cursor so no rows are missed or duplicated across the switch.
	StreamLogs(context.Context, *connect.Request[v1.StreamLogsRequest]) (*connect.ServerStreamForClient[v1.StreamLogsResponse], error)
}

// NewLogsServiceClient constructs a client for the core.observability.logs.v1.LogsService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewLogsServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) LogsServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &logsServiceClient{
		queryLogs: connect.NewClient[v1.QueryLogsRequest, v1.QueryLogsResponse](
			httpClient,
			baseURL+LogsServiceQueryLogsProcedure,
			connect.WithSchema(logsServiceQueryLogsMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		streamLogs: connect.NewClient[v1.StreamLogsRequest, v1.StreamLogsResponse](
			httpClient,
			baseURL+LogsServiceStreamLogsProcedure,
			connect.WithSchema(logsServiceStreamLogsMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// logsServiceClient implements LogsServiceClient.
type logsServiceClient struct {
	queryLogs  *connect.Client[v1.QueryLogsRequest, v1.QueryLogsResponse]
	streamLogs *connect.Client[v1.StreamLogsRequest, v1.StreamLogsResponse]
}

// QueryLogs calls core.observability.logs.v1.LogsService.QueryLogs.
func (c *logsServiceClient) QueryLogs(ctx context.Context, req *connect.Request[v1.QueryLogsRequest]) (*connect.Response[v1.QueryLogsResponse], error) {
	return c.queryLogs.CallUnary(ctx, req)
}

// StreamLogs calls core.observability.logs.v1.LogsService.StreamLogs.
func (c *logsServiceClient) StreamLogs(ctx context.Context, req *connect.Request[v1.StreamLogsRequest]) (*connect.ServerStreamForClient[v1.StreamLogsResponse], error) {
	return c.streamLogs.CallServerStream(ctx, req)
}

// LogsServiceHandler is an implementation of the core.observability.logs.v1.LogsService service.
type LogsServiceHandler interface {
	// QueryLogs returns a bounded set of log rows matching an optional BeaconQL
	// filter expression, most recent first.
	QueryLogs(context.Context, *connect.Request[v1.QueryLogsRequest]) (*connect.Response[v1.QueryLogsResponse], error)
	// StreamLogs opens a persistent server-streaming RPC. The server first replays
	// historical rows matching `filter` (bounded by `limit`, optionally starting
	// after the `after` cursor), then continues streaming newly-ingested rows
	// matching the same filter as they arrive. The stream stays open until the
	// client closes it; the client re-issues the RPC with a new `filter` to change
	// the active filter, using the last received row's timestamp as the new
	// `after` cursor so no rows are missed or duplicated across the switch.
	StreamLogs(context.Context, *connect.Request[v1.StreamLogsRequest], *connect.ServerStream[v1.StreamLogsResponse]) error
}

// NewLogsServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewLogsServiceHandler(svc LogsServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	logsServiceQueryLogsHandler := connect.NewUnaryHandler(
		LogsServiceQueryLogsProcedure,
		svc.QueryLogs,
		connect.WithSchema(logsServiceQueryLogsMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	logsServiceStreamLogsHandler := connect.NewServerStreamHandler(
		LogsServiceStreamLogsProcedure,
		svc.StreamLogs,
		connect.WithSchema(logsServiceStreamLogsMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/core.observability.logs.v1.LogsService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case LogsServiceQueryLogsProcedure:
			logsServiceQueryLogsHandler.ServeHTTP(w, r)
		case LogsServiceStreamLogsProcedure:
			logsServiceStreamLogsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedLogsServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedLogsServiceHandler struct{}

func (UnimplementedLogsServiceHandler) QueryLogs(context.Context, *connect.Request[v1.QueryLogsRequest]) (*connect.Response[v1.QueryLogsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("core.observability.logs.v1.LogsService.QueryLogs is not implemented"))
}

func (UnimplementedLogsServiceHandler) StreamLogs(context.Context, *connect.Request[v1.StreamLogsRequest], *connect.ServerStream[v1.StreamLogsResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("core.observability.logs.v1.LogsService.StreamLogs is not implemented"))
}
