// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: core/observability/traces/v1/traces.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/steady-bytes/draft/api/core/observability/traces/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TracesServiceName is the fully-qualified name of the TracesService service.
	TracesServiceName = "core.observability.traces.v1.TracesService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TracesServiceSearchTracesProcedure is the fully-qualified name of the TracesService's
	// SearchTraces RPC.
	TracesServiceSearchTracesProcedure = "/core.observability.traces.v1.TracesService/SearchTraces"
	// TracesServiceGetTraceProcedure is the fully-qualified name of the TracesService's GetTrace RPC.
	TracesServiceGetTraceProcedure = "/core.observability.traces.v1.TracesService/GetTrace"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	tracesServiceServiceDescriptor            = v1.File_core_observability_traces_v1_traces_proto.Services().ByName("TracesService")
	tracesServiceSearchTracesMethodDescriptor = tracesServiceServiceDescriptor.Methods().ByName("SearchTraces")
	tracesServiceGetTraceMethodDescriptor     = tracesServiceServiceDescriptor.Methods().ByName("GetTrace")
)

// TracesServiceClient is a client for the core.observability.traces.v1.TracesService service.
type TracesServiceClient interface {
	// SearchTraces returns a bounded, paginated set of trace roots matching an
	// optional BeaconQL filter expression (compiled against the `trace_roots`
	// materialized view's columns — see query/beaconql.go's CompileTraceRoot),
	// most recent first.
	SearchTraces(context.Context, *connect.Request[v1.SearchTracesRequest]) (*connect.Response[v1.SearchTracesResponse], error)
	// GetTrace returns every span belonging to one trace_id, ordered so a
	// flame graph can be assembled from parent/child relationships (by
	// start_time ascending, so a parent always precedes its children).
	GetTrace(context.Context, *connect.Request[v1.GetTraceRequest]) (*connect.Response[v1.GetTraceResponse], error)
}

// NewTracesServiceClient constructs a client for the core.observability.traces.v1.TracesService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTracesServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TracesServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &tracesServiceClient{
		searchTraces: connect.NewClient[v1.SearchTracesRequest, v1.SearchTracesResponse](
			httpClient,
			baseURL+TracesServiceSearchTracesProcedure,
			connect.WithSchema(tracesServiceSearchTracesMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		getTrace: connect.NewClient[v1.GetTraceRequest, v1.GetTraceResponse](
			httpClient,
			baseURL+TracesServiceGetTraceProcedure,
			connect.WithSchema(tracesServiceGetTraceMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// tracesServiceClient implements TracesServiceClient.
type tracesServiceClient struct {
	searchTraces *connect.Client[v1.SearchTracesRequest, v1.SearchTracesResponse]
	getTrace     *connect.Client[v1.GetTraceRequest, v1.GetTraceResponse]
}

// SearchTraces calls core.observability.traces.v1.TracesService.SearchTraces.
func (c *tracesServiceClient) SearchTraces(ctx context.Context, req *connect.Request[v1.SearchTracesRequest]) (*connect.Response[v1.SearchTracesResponse], error) {
	return c.searchTraces.CallUnary(ctx, req)
}

// GetTrace calls core.observability.traces.v1.TracesService.GetTrace.
func (c *tracesServiceClient) GetTrace(ctx context.Context, req *connect.Request[v1.GetTraceRequest]) (*connect.Response[v1.GetTraceResponse], error) {
	return c.getTrace.CallUnary(ctx, req)
}

// TracesServiceHandler is an implementation of the core.observability.traces.v1.TracesService
// service.
type TracesServiceHandler interface {
	// SearchTraces returns a bounded, paginated set of trace roots matching an
	// optional BeaconQL filter expression (compiled against the `trace_roots`
	// materialized view's columns — see query/beaconql.go's CompileTraceRoot),
	// most recent first.
	SearchTraces(context.Context, *connect.Request[v1.SearchTracesRequest]) (*connect.Response[v1.SearchTracesResponse], error)
	// GetTrace returns every span belonging to one trace_id, ordered so a
	// flame graph can be assembled from parent/child relationships (by
	// start_time ascending, so a parent always precedes its children).
	GetTrace(context.Context, *connect.Request[v1.GetTraceRequest]) (*connect.Response[v1.GetTraceResponse], error)
}

// NewTracesServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTracesServiceHandler(svc TracesServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	tracesServiceSearchTracesHandler := connect.NewUnaryHandler(
		TracesServiceSearchTracesProcedure,
		svc.SearchTraces,
		connect.WithSchema(tracesServiceSearchTracesMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	tracesServiceGetTraceHandler := connect.NewUnaryHandler(
		TracesServiceGetTraceProcedure,
		svc.GetTrace,
		connect.WithSchema(tracesServiceGetTraceMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/core.observability.traces.v1.TracesService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TracesServiceSearchTracesProcedure:
			tracesServiceSearchTracesHandler.ServeHTTP(w, r)
		case TracesServiceGetTraceProcedure:
			tracesServiceGetTraceHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTracesServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedTracesServiceHandler struct{}

func (UnimplementedTracesServiceHandler) SearchTraces(context.Context, *connect.Request[v1.SearchTracesRequest]) (*connect.Response[v1.SearchTracesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("core.observability.traces.v1.TracesService.SearchTraces is not implemented"))
}

func (UnimplementedTracesServiceHandler) GetTrace(context.Context, *connect.Request[v1.GetTraceRequest]) (*connect.Response[v1.GetTraceResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("core.observability.traces.v1.TracesService.GetTrace is not implemented"))
}
