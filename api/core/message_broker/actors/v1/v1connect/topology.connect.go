// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: core/message_broker/actors/v1/topology.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/steady-bytes/draft/api/core/message_broker/actors/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// TopologyName is the fully-qualified name of the Topology service.
	TopologyName = "core.message_broker.actors.v1.Topology"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// TopologyGetTopologyProcedure is the fully-qualified name of the Topology's GetTopology RPC.
	TopologyGetTopologyProcedure = "/core.message_broker.actors.v1.Topology/GetTopology"
	// TopologyWatchTopologyProcedure is the fully-qualified name of the Topology's WatchTopology RPC.
	TopologyWatchTopologyProcedure = "/core.message_broker.actors.v1.Topology/WatchTopology"
)

// TopologyClient is a client for the core.message_broker.actors.v1.Topology service.
type TopologyClient interface {
	// GetTopology returns a snapshot of the current event topology: producers
	// derived from ClickHouse distinct sources, consumers and edges derived from
	// live ConsumeRequest registrations tracked in the broker's atomicMap.
	GetTopology(context.Context, *connect.Request[v1.GetTopologyRequest]) (*connect.Response[v1.GetTopologyResponse], error)
	// WatchTopology streams incremental topology deltas as consumers connect or
	// disconnect and as new producer sources appear in the event store.
	WatchTopology(context.Context, *connect.Request[v1.WatchTopologyRequest]) (*connect.ServerStreamForClient[v1.WatchTopologyResponse], error)
}

// NewTopologyClient constructs a client for the core.message_broker.actors.v1.Topology service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewTopologyClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) TopologyClient {
	baseURL = strings.TrimRight(baseURL, "/")
	topologyMethods := v1.File_core_message_broker_actors_v1_topology_proto.Services().ByName("Topology").Methods()
	return &topologyClient{
		getTopology: connect.NewClient[v1.GetTopologyRequest, v1.GetTopologyResponse](
			httpClient,
			baseURL+TopologyGetTopologyProcedure,
			connect.WithSchema(topologyMethods.ByName("GetTopology")),
			connect.WithClientOptions(opts...),
		),
		watchTopology: connect.NewClient[v1.WatchTopologyRequest, v1.WatchTopologyResponse](
			httpClient,
			baseURL+TopologyWatchTopologyProcedure,
			connect.WithSchema(topologyMethods.ByName("WatchTopology")),
			connect.WithClientOptions(opts...),
		),
	}
}

// topologyClient implements TopologyClient.
type topologyClient struct {
	getTopology   *connect.Client[v1.GetTopologyRequest, v1.GetTopologyResponse]
	watchTopology *connect.Client[v1.WatchTopologyRequest, v1.WatchTopologyResponse]
}

// GetTopology calls core.message_broker.actors.v1.Topology.GetTopology.
func (c *topologyClient) GetTopology(ctx context.Context, req *connect.Request[v1.GetTopologyRequest]) (*connect.Response[v1.GetTopologyResponse], error) {
	return c.getTopology.CallUnary(ctx, req)
}

// WatchTopology calls core.message_broker.actors.v1.Topology.WatchTopology.
func (c *topologyClient) WatchTopology(ctx context.Context, req *connect.Request[v1.WatchTopologyRequest]) (*connect.ServerStreamForClient[v1.WatchTopologyResponse], error) {
	return c.watchTopology.CallServerStream(ctx, req)
}

// TopologyHandler is an implementation of the core.message_broker.actors.v1.Topology service.
type TopologyHandler interface {
	// GetTopology returns a snapshot of the current event topology: producers
	// derived from ClickHouse distinct sources, consumers and edges derived from
	// live ConsumeRequest registrations tracked in the broker's atomicMap.
	GetTopology(context.Context, *connect.Request[v1.GetTopologyRequest]) (*connect.Response[v1.GetTopologyResponse], error)
	// WatchTopology streams incremental topology deltas as consumers connect or
	// disconnect and as new producer sources appear in the event store.
	WatchTopology(context.Context, *connect.Request[v1.WatchTopologyRequest], *connect.ServerStream[v1.WatchTopologyResponse]) error
}

// NewTopologyHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewTopologyHandler(svc TopologyHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	topologyMethods := v1.File_core_message_broker_actors_v1_topology_proto.Services().ByName("Topology").Methods()
	topologyGetTopologyHandler := connect.NewUnaryHandler(
		TopologyGetTopologyProcedure,
		svc.GetTopology,
		connect.WithSchema(topologyMethods.ByName("GetTopology")),
		connect.WithHandlerOptions(opts...),
	)
	topologyWatchTopologyHandler := connect.NewServerStreamHandler(
		TopologyWatchTopologyProcedure,
		svc.WatchTopology,
		connect.WithSchema(topologyMethods.ByName("WatchTopology")),
		connect.WithHandlerOptions(opts...),
	)
	return "/core.message_broker.actors.v1.Topology/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case TopologyGetTopologyProcedure:
			topologyGetTopologyHandler.ServeHTTP(w, r)
		case TopologyWatchTopologyProcedure:
			topologyWatchTopologyHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedTopologyHandler returns CodeUnimplemented from all methods.
type UnimplementedTopologyHandler struct{}

func (UnimplementedTopologyHandler) GetTopology(context.Context, *connect.Request[v1.GetTopologyRequest]) (*connect.Response[v1.GetTopologyResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("core.message_broker.actors.v1.Topology.GetTopology is not implemented"))
}

func (UnimplementedTopologyHandler) WatchTopology(context.Context, *connect.Request[v1.WatchTopologyRequest], *connect.ServerStream[v1.WatchTopologyResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("core.message_broker.actors.v1.Topology.WatchTopology is not implemented"))
}
