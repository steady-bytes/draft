// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: core/message_broker/actors/v1/query.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/steady-bytes/draft/api/core/message_broker/actors/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// QueryName is the fully-qualified name of the Query service.
	QueryName = "core.message_broker.actors.v1.Query"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// QueryQueryProcedure is the fully-qualified name of the Query's Query RPC.
	QueryQueryProcedure = "/core.message_broker.actors.v1.Query/Query"
	// QueryQueryStreamProcedure is the fully-qualified name of the Query's QueryStream RPC.
	QueryQueryStreamProcedure = "/core.message_broker.actors.v1.Query/QueryStream"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	queryServiceDescriptor           = v1.File_core_message_broker_actors_v1_query_proto.Services().ByName("Query")
	queryQueryMethodDescriptor       = queryServiceDescriptor.Methods().ByName("Query")
	queryQueryStreamMethodDescriptor = queryServiceDescriptor.Methods().ByName("QueryStream")
)

// QueryClient is a client for the core.message_broker.actors.v1.Query service.
type QueryClient interface {
	// Query executes a CESQL expression against stored events and returns all
	// matching results in a single response.
	Query(context.Context, *connect.Request[v1.QueryRequest]) (*connect.Response[v1.QueryResponse], error)
	// QueryStream opens a persistent server-sent stream. The server first
	// replays any stored events that match the expression (subject to `limit`
	// and `after`), then continues streaming live events as they flow through
	// catalyst in real time. The stream stays open until the client closes it.
	QueryStream(context.Context, *connect.Request[v1.QueryRequest]) (*connect.ServerStreamForClient[v1.QueryStreamResponse], error)
}

// NewQueryClient constructs a client for the core.message_broker.actors.v1.Query service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewQueryClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) QueryClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &queryClient{
		query: connect.NewClient[v1.QueryRequest, v1.QueryResponse](
			httpClient,
			baseURL+QueryQueryProcedure,
			connect.WithSchema(queryQueryMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		queryStream: connect.NewClient[v1.QueryRequest, v1.QueryStreamResponse](
			httpClient,
			baseURL+QueryQueryStreamProcedure,
			connect.WithSchema(queryQueryStreamMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// queryClient implements QueryClient.
type queryClient struct {
	query       *connect.Client[v1.QueryRequest, v1.QueryResponse]
	queryStream *connect.Client[v1.QueryRequest, v1.QueryStreamResponse]
}

// Query calls core.message_broker.actors.v1.Query.Query.
func (c *queryClient) Query(ctx context.Context, req *connect.Request[v1.QueryRequest]) (*connect.Response[v1.QueryResponse], error) {
	return c.query.CallUnary(ctx, req)
}

// QueryStream calls core.message_broker.actors.v1.Query.QueryStream.
func (c *queryClient) QueryStream(ctx context.Context, req *connect.Request[v1.QueryRequest]) (*connect.ServerStreamForClient[v1.QueryStreamResponse], error) {
	return c.queryStream.CallServerStream(ctx, req)
}

// QueryHandler is an implementation of the core.message_broker.actors.v1.Query service.
type QueryHandler interface {
	// Query executes a CESQL expression against stored events and returns all
	// matching results in a single response.
	Query(context.Context, *connect.Request[v1.QueryRequest]) (*connect.Response[v1.QueryResponse], error)
	// QueryStream opens a persistent server-sent stream. The server first
	// replays any stored events that match the expression (subject to `limit`
	// and `after`), then continues streaming live events as they flow through
	// catalyst in real time. The stream stays open until the client closes it.
	QueryStream(context.Context, *connect.Request[v1.QueryRequest], *connect.ServerStream[v1.QueryStreamResponse]) error
}

// NewQueryHandler builds an HTTP handler from the service implementation. It returns the path on
// which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewQueryHandler(svc QueryHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	queryQueryHandler := connect.NewUnaryHandler(
		QueryQueryProcedure,
		svc.Query,
		connect.WithSchema(queryQueryMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	queryQueryStreamHandler := connect.NewServerStreamHandler(
		QueryQueryStreamProcedure,
		svc.QueryStream,
		connect.WithSchema(queryQueryStreamMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/core.message_broker.actors.v1.Query/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case QueryQueryProcedure:
			queryQueryHandler.ServeHTTP(w, r)
		case QueryQueryStreamProcedure:
			queryQueryStreamHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedQueryHandler returns CodeUnimplemented from all methods.
type UnimplementedQueryHandler struct{}

func (UnimplementedQueryHandler) Query(context.Context, *connect.Request[v1.QueryRequest]) (*connect.Response[v1.QueryResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("core.message_broker.actors.v1.Query.Query is not implemented"))
}

func (UnimplementedQueryHandler) QueryStream(context.Context, *connect.Request[v1.QueryRequest], *connect.ServerStream[v1.QueryStreamResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("core.message_broker.actors.v1.Query.QueryStream is not implemented"))
}
