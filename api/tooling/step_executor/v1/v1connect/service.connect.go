// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tooling/step_executor/v1/service.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/steady-bytes/draft/api/tooling/step_executor/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// StepExecutorName is the fully-qualified name of the StepExecutor service.
	StepExecutorName = "tooling.step_executor.v1.StepExecutor"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// StepExecutorExecuteProcedure is the fully-qualified name of the StepExecutor's Execute RPC.
	StepExecutorExecuteProcedure = "/tooling.step_executor.v1.StepExecutor/Execute"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	stepExecutorServiceDescriptor       = v1.File_tooling_step_executor_v1_service_proto.Services().ByName("StepExecutor")
	stepExecutorExecuteMethodDescriptor = stepExecutorServiceDescriptor.Methods().ByName("Execute")
)

// StepExecutorClient is a client for the tooling.step_executor.v1.StepExecutor service.
type StepExecutorClient interface {
	Execute(context.Context, *connect.Request[v1.StepRequest]) (*connect.Response[v1.StepResponse], error)
}

// NewStepExecutorClient constructs a client for the tooling.step_executor.v1.StepExecutor service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewStepExecutorClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) StepExecutorClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &stepExecutorClient{
		execute: connect.NewClient[v1.StepRequest, v1.StepResponse](
			httpClient,
			baseURL+StepExecutorExecuteProcedure,
			connect.WithSchema(stepExecutorExecuteMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// stepExecutorClient implements StepExecutorClient.
type stepExecutorClient struct {
	execute *connect.Client[v1.StepRequest, v1.StepResponse]
}

// Execute calls tooling.step_executor.v1.StepExecutor.Execute.
func (c *stepExecutorClient) Execute(ctx context.Context, req *connect.Request[v1.StepRequest]) (*connect.Response[v1.StepResponse], error) {
	return c.execute.CallUnary(ctx, req)
}

// StepExecutorHandler is an implementation of the tooling.step_executor.v1.StepExecutor service.
type StepExecutorHandler interface {
	Execute(context.Context, *connect.Request[v1.StepRequest]) (*connect.Response[v1.StepResponse], error)
}

// NewStepExecutorHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewStepExecutorHandler(svc StepExecutorHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	stepExecutorExecuteHandler := connect.NewUnaryHandler(
		StepExecutorExecuteProcedure,
		svc.Execute,
		connect.WithSchema(stepExecutorExecuteMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/tooling.step_executor.v1.StepExecutor/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case StepExecutorExecuteProcedure:
			stepExecutorExecuteHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedStepExecutorHandler returns CodeUnimplemented from all methods.
type UnimplementedStepExecutorHandler struct{}

func (UnimplementedStepExecutorHandler) Execute(context.Context, *connect.Request[v1.StepRequest]) (*connect.Response[v1.StepResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tooling.step_executor.v1.StepExecutor.Execute is not implemented"))
}
