// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tooling/workflow/v1/service.proto

package v1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/steady-bytes/draft/api/tooling/workflow/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// WorkflowServiceName is the fully-qualified name of the WorkflowService service.
	WorkflowServiceName = "tooling.workflow.v1.WorkflowService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// WorkflowServiceTriggerRunProcedure is the fully-qualified name of the WorkflowService's
	// TriggerRun RPC.
	WorkflowServiceTriggerRunProcedure = "/tooling.workflow.v1.WorkflowService/TriggerRun"
	// WorkflowServiceGetRunProcedure is the fully-qualified name of the WorkflowService's GetRun RPC.
	WorkflowServiceGetRunProcedure = "/tooling.workflow.v1.WorkflowService/GetRun"
	// WorkflowServiceListRunsProcedure is the fully-qualified name of the WorkflowService's ListRuns
	// RPC.
	WorkflowServiceListRunsProcedure = "/tooling.workflow.v1.WorkflowService/ListRuns"
	// WorkflowServiceListWorkflowsProcedure is the fully-qualified name of the WorkflowService's
	// ListWorkflows RPC.
	WorkflowServiceListWorkflowsProcedure = "/tooling.workflow.v1.WorkflowService/ListWorkflows"
	// WorkflowServiceCreateWorkflowProcedure is the fully-qualified name of the WorkflowService's
	// CreateWorkflow RPC.
	WorkflowServiceCreateWorkflowProcedure = "/tooling.workflow.v1.WorkflowService/CreateWorkflow"
	// WorkflowServiceUpdateWorkflowProcedure is the fully-qualified name of the WorkflowService's
	// UpdateWorkflow RPC.
	WorkflowServiceUpdateWorkflowProcedure = "/tooling.workflow.v1.WorkflowService/UpdateWorkflow"
	// WorkflowServiceDeleteWorkflowProcedure is the fully-qualified name of the WorkflowService's
	// DeleteWorkflow RPC.
	WorkflowServiceDeleteWorkflowProcedure = "/tooling.workflow.v1.WorkflowService/DeleteWorkflow"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	workflowServiceServiceDescriptor              = v1.File_tooling_workflow_v1_service_proto.Services().ByName("WorkflowService")
	workflowServiceTriggerRunMethodDescriptor     = workflowServiceServiceDescriptor.Methods().ByName("TriggerRun")
	workflowServiceGetRunMethodDescriptor         = workflowServiceServiceDescriptor.Methods().ByName("GetRun")
	workflowServiceListRunsMethodDescriptor       = workflowServiceServiceDescriptor.Methods().ByName("ListRuns")
	workflowServiceListWorkflowsMethodDescriptor  = workflowServiceServiceDescriptor.Methods().ByName("ListWorkflows")
	workflowServiceCreateWorkflowMethodDescriptor = workflowServiceServiceDescriptor.Methods().ByName("CreateWorkflow")
	workflowServiceUpdateWorkflowMethodDescriptor = workflowServiceServiceDescriptor.Methods().ByName("UpdateWorkflow")
	workflowServiceDeleteWorkflowMethodDescriptor = workflowServiceServiceDescriptor.Methods().ByName("DeleteWorkflow")
)

// WorkflowServiceClient is a client for the tooling.workflow.v1.WorkflowService service.
type WorkflowServiceClient interface {
	// TriggerRun starts a run of a workflow manually (the UI's "run now"
	// path) — the same execution path a webhook-triggered run takes, just a
	// different entry point.
	TriggerRun(context.Context, *connect.Request[v1.TriggerRunRequest]) (*connect.Response[v1.TriggerRunResponse], error)
	// GetRun returns the current status of one run, polled by run_id.
	GetRun(context.Context, *connect.Request[v1.GetRunRequest]) (*connect.Response[v1.Run], error)
	// ListRuns returns run history, optionally filtered to one workflow.
	ListRuns(context.Context, *connect.Request[v1.ListRunsRequest]) (*connect.Response[v1.ListRunsResponse], error)
	// ListWorkflows returns every configured workflow.
	ListWorkflows(context.Context, *connect.Request[v1.ListWorkflowsRequest]) (*connect.Response[v1.ListWorkflowsResponse], error)
	// CreateWorkflow persists a new workflow from a raw YAML document (the
	// same shape as a workflows_dir file's contents), parsed and validated
	// server-side. Fails if a workflow with the same name already exists —
	// see docs/website/content/docs/architecture/bench-workflow-engine.md's
	// "Authoring workflows without a file" for why this is a real create,
	// not the storage layer's own unconditional upsert.
	CreateWorkflow(context.Context, *connect.Request[v1.CreateWorkflowRequest]) (*connect.Response[v1.CreateWorkflowResponse], error)
	// UpdateWorkflow replaces an existing workflow's definition. Fails if
	// name doesn't already exist, or if yaml's own metadata.name doesn't
	// match name — a rename isn't supported (see the doc's Open questions).
	UpdateWorkflow(context.Context, *connect.Request[v1.UpdateWorkflowRequest]) (*connect.Response[v1.UpdateWorkflowResponse], error)
	// DeleteWorkflow removes a workflow's definition. Its run history is
	// untouched — runs are keyed by workflow_name as a plain string, not a
	// foreign key, so deleting the definition doesn't delete what it ran.
	DeleteWorkflow(context.Context, *connect.Request[v1.DeleteWorkflowRequest]) (*connect.Response[v1.DeleteWorkflowResponse], error)
}

// NewWorkflowServiceClient constructs a client for the tooling.workflow.v1.WorkflowService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewWorkflowServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) WorkflowServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &workflowServiceClient{
		triggerRun: connect.NewClient[v1.TriggerRunRequest, v1.TriggerRunResponse](
			httpClient,
			baseURL+WorkflowServiceTriggerRunProcedure,
			connect.WithSchema(workflowServiceTriggerRunMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		getRun: connect.NewClient[v1.GetRunRequest, v1.Run](
			httpClient,
			baseURL+WorkflowServiceGetRunProcedure,
			connect.WithSchema(workflowServiceGetRunMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		listRuns: connect.NewClient[v1.ListRunsRequest, v1.ListRunsResponse](
			httpClient,
			baseURL+WorkflowServiceListRunsProcedure,
			connect.WithSchema(workflowServiceListRunsMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		listWorkflows: connect.NewClient[v1.ListWorkflowsRequest, v1.ListWorkflowsResponse](
			httpClient,
			baseURL+WorkflowServiceListWorkflowsProcedure,
			connect.WithSchema(workflowServiceListWorkflowsMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		createWorkflow: connect.NewClient[v1.CreateWorkflowRequest, v1.CreateWorkflowResponse](
			httpClient,
			baseURL+WorkflowServiceCreateWorkflowProcedure,
			connect.WithSchema(workflowServiceCreateWorkflowMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		updateWorkflow: connect.NewClient[v1.UpdateWorkflowRequest, v1.UpdateWorkflowResponse](
			httpClient,
			baseURL+WorkflowServiceUpdateWorkflowProcedure,
			connect.WithSchema(workflowServiceUpdateWorkflowMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
		deleteWorkflow: connect.NewClient[v1.DeleteWorkflowRequest, v1.DeleteWorkflowResponse](
			httpClient,
			baseURL+WorkflowServiceDeleteWorkflowProcedure,
			connect.WithSchema(workflowServiceDeleteWorkflowMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// workflowServiceClient implements WorkflowServiceClient.
type workflowServiceClient struct {
	triggerRun     *connect.Client[v1.TriggerRunRequest, v1.TriggerRunResponse]
	getRun         *connect.Client[v1.GetRunRequest, v1.Run]
	listRuns       *connect.Client[v1.ListRunsRequest, v1.ListRunsResponse]
	listWorkflows  *connect.Client[v1.ListWorkflowsRequest, v1.ListWorkflowsResponse]
	createWorkflow *connect.Client[v1.CreateWorkflowRequest, v1.CreateWorkflowResponse]
	updateWorkflow *connect.Client[v1.UpdateWorkflowRequest, v1.UpdateWorkflowResponse]
	deleteWorkflow *connect.Client[v1.DeleteWorkflowRequest, v1.DeleteWorkflowResponse]
}

// TriggerRun calls tooling.workflow.v1.WorkflowService.TriggerRun.
func (c *workflowServiceClient) TriggerRun(ctx context.Context, req *connect.Request[v1.TriggerRunRequest]) (*connect.Response[v1.TriggerRunResponse], error) {
	return c.triggerRun.CallUnary(ctx, req)
}

// GetRun calls tooling.workflow.v1.WorkflowService.GetRun.
func (c *workflowServiceClient) GetRun(ctx context.Context, req *connect.Request[v1.GetRunRequest]) (*connect.Response[v1.Run], error) {
	return c.getRun.CallUnary(ctx, req)
}

// ListRuns calls tooling.workflow.v1.WorkflowService.ListRuns.
func (c *workflowServiceClient) ListRuns(ctx context.Context, req *connect.Request[v1.ListRunsRequest]) (*connect.Response[v1.ListRunsResponse], error) {
	return c.listRuns.CallUnary(ctx, req)
}

// ListWorkflows calls tooling.workflow.v1.WorkflowService.ListWorkflows.
func (c *workflowServiceClient) ListWorkflows(ctx context.Context, req *connect.Request[v1.ListWorkflowsRequest]) (*connect.Response[v1.ListWorkflowsResponse], error) {
	return c.listWorkflows.CallUnary(ctx, req)
}

// CreateWorkflow calls tooling.workflow.v1.WorkflowService.CreateWorkflow.
func (c *workflowServiceClient) CreateWorkflow(ctx context.Context, req *connect.Request[v1.CreateWorkflowRequest]) (*connect.Response[v1.CreateWorkflowResponse], error) {
	return c.createWorkflow.CallUnary(ctx, req)
}

// UpdateWorkflow calls tooling.workflow.v1.WorkflowService.UpdateWorkflow.
func (c *workflowServiceClient) UpdateWorkflow(ctx context.Context, req *connect.Request[v1.UpdateWorkflowRequest]) (*connect.Response[v1.UpdateWorkflowResponse], error) {
	return c.updateWorkflow.CallUnary(ctx, req)
}

// DeleteWorkflow calls tooling.workflow.v1.WorkflowService.DeleteWorkflow.
func (c *workflowServiceClient) DeleteWorkflow(ctx context.Context, req *connect.Request[v1.DeleteWorkflowRequest]) (*connect.Response[v1.DeleteWorkflowResponse], error) {
	return c.deleteWorkflow.CallUnary(ctx, req)
}

// WorkflowServiceHandler is an implementation of the tooling.workflow.v1.WorkflowService service.
type WorkflowServiceHandler interface {
	// TriggerRun starts a run of a workflow manually (the UI's "run now"
	// path) — the same execution path a webhook-triggered run takes, just a
	// different entry point.
	TriggerRun(context.Context, *connect.Request[v1.TriggerRunRequest]) (*connect.Response[v1.TriggerRunResponse], error)
	// GetRun returns the current status of one run, polled by run_id.
	GetRun(context.Context, *connect.Request[v1.GetRunRequest]) (*connect.Response[v1.Run], error)
	// ListRuns returns run history, optionally filtered to one workflow.
	ListRuns(context.Context, *connect.Request[v1.ListRunsRequest]) (*connect.Response[v1.ListRunsResponse], error)
	// ListWorkflows returns every configured workflow.
	ListWorkflows(context.Context, *connect.Request[v1.ListWorkflowsRequest]) (*connect.Response[v1.ListWorkflowsResponse], error)
	// CreateWorkflow persists a new workflow from a raw YAML document (the
	// same shape as a workflows_dir file's contents), parsed and validated
	// server-side. Fails if a workflow with the same name already exists —
	// see docs/website/content/docs/architecture/bench-workflow-engine.md's
	// "Authoring workflows without a file" for why this is a real create,
	// not the storage layer's own unconditional upsert.
	CreateWorkflow(context.Context, *connect.Request[v1.CreateWorkflowRequest]) (*connect.Response[v1.CreateWorkflowResponse], error)
	// UpdateWorkflow replaces an existing workflow's definition. Fails if
	// name doesn't already exist, or if yaml's own metadata.name doesn't
	// match name — a rename isn't supported (see the doc's Open questions).
	UpdateWorkflow(context.Context, *connect.Request[v1.UpdateWorkflowRequest]) (*connect.Response[v1.UpdateWorkflowResponse], error)
	// DeleteWorkflow removes a workflow's definition. Its run history is
	// untouched — runs are keyed by workflow_name as a plain string, not a
	// foreign key, so deleting the definition doesn't delete what it ran.
	DeleteWorkflow(context.Context, *connect.Request[v1.DeleteWorkflowRequest]) (*connect.Response[v1.DeleteWorkflowResponse], error)
}

// NewWorkflowServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewWorkflowServiceHandler(svc WorkflowServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	workflowServiceTriggerRunHandler := connect.NewUnaryHandler(
		WorkflowServiceTriggerRunProcedure,
		svc.TriggerRun,
		connect.WithSchema(workflowServiceTriggerRunMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	workflowServiceGetRunHandler := connect.NewUnaryHandler(
		WorkflowServiceGetRunProcedure,
		svc.GetRun,
		connect.WithSchema(workflowServiceGetRunMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	workflowServiceListRunsHandler := connect.NewUnaryHandler(
		WorkflowServiceListRunsProcedure,
		svc.ListRuns,
		connect.WithSchema(workflowServiceListRunsMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	workflowServiceListWorkflowsHandler := connect.NewUnaryHandler(
		WorkflowServiceListWorkflowsProcedure,
		svc.ListWorkflows,
		connect.WithSchema(workflowServiceListWorkflowsMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	workflowServiceCreateWorkflowHandler := connect.NewUnaryHandler(
		WorkflowServiceCreateWorkflowProcedure,
		svc.CreateWorkflow,
		connect.WithSchema(workflowServiceCreateWorkflowMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	workflowServiceUpdateWorkflowHandler := connect.NewUnaryHandler(
		WorkflowServiceUpdateWorkflowProcedure,
		svc.UpdateWorkflow,
		connect.WithSchema(workflowServiceUpdateWorkflowMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	workflowServiceDeleteWorkflowHandler := connect.NewUnaryHandler(
		WorkflowServiceDeleteWorkflowProcedure,
		svc.DeleteWorkflow,
		connect.WithSchema(workflowServiceDeleteWorkflowMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/tooling.workflow.v1.WorkflowService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case WorkflowServiceTriggerRunProcedure:
			workflowServiceTriggerRunHandler.ServeHTTP(w, r)
		case WorkflowServiceGetRunProcedure:
			workflowServiceGetRunHandler.ServeHTTP(w, r)
		case WorkflowServiceListRunsProcedure:
			workflowServiceListRunsHandler.ServeHTTP(w, r)
		case WorkflowServiceListWorkflowsProcedure:
			workflowServiceListWorkflowsHandler.ServeHTTP(w, r)
		case WorkflowServiceCreateWorkflowProcedure:
			workflowServiceCreateWorkflowHandler.ServeHTTP(w, r)
		case WorkflowServiceUpdateWorkflowProcedure:
			workflowServiceUpdateWorkflowHandler.ServeHTTP(w, r)
		case WorkflowServiceDeleteWorkflowProcedure:
			workflowServiceDeleteWorkflowHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedWorkflowServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedWorkflowServiceHandler struct{}

func (UnimplementedWorkflowServiceHandler) TriggerRun(context.Context, *connect.Request[v1.TriggerRunRequest]) (*connect.Response[v1.TriggerRunResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tooling.workflow.v1.WorkflowService.TriggerRun is not implemented"))
}

func (UnimplementedWorkflowServiceHandler) GetRun(context.Context, *connect.Request[v1.GetRunRequest]) (*connect.Response[v1.Run], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tooling.workflow.v1.WorkflowService.GetRun is not implemented"))
}

func (UnimplementedWorkflowServiceHandler) ListRuns(context.Context, *connect.Request[v1.ListRunsRequest]) (*connect.Response[v1.ListRunsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tooling.workflow.v1.WorkflowService.ListRuns is not implemented"))
}

func (UnimplementedWorkflowServiceHandler) ListWorkflows(context.Context, *connect.Request[v1.ListWorkflowsRequest]) (*connect.Response[v1.ListWorkflowsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tooling.workflow.v1.WorkflowService.ListWorkflows is not implemented"))
}

func (UnimplementedWorkflowServiceHandler) CreateWorkflow(context.Context, *connect.Request[v1.CreateWorkflowRequest]) (*connect.Response[v1.CreateWorkflowResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tooling.workflow.v1.WorkflowService.CreateWorkflow is not implemented"))
}

func (UnimplementedWorkflowServiceHandler) UpdateWorkflow(context.Context, *connect.Request[v1.UpdateWorkflowRequest]) (*connect.Response[v1.UpdateWorkflowResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tooling.workflow.v1.WorkflowService.UpdateWorkflow is not implemented"))
}

func (UnimplementedWorkflowServiceHandler) DeleteWorkflow(context.Context, *connect.Request[v1.DeleteWorkflowRequest]) (*connect.Response[v1.DeleteWorkflowResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tooling.workflow.v1.WorkflowService.DeleteWorkflow is not implemented"))
}
